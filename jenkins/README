In the run command, you must fill in github credentials
(github --> settings --> developer settings --> OAuth Apps)
 - github client ID
 - github client secret

In the gui, you must fill in:
 - the admin username and password (prompted)
 - the ssh key for controller-agent communication (global credentials)
 - the github personal access token as username/password (global credentials)

USAGE:
docker build -t jenkins:jcasc .
docker run --name jenkins --rm --network jenkins --publish 8080:8080 --publish 5000:5000 jenkins:jcasc