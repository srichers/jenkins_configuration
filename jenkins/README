In the gui, you must fill in:
 - the admin password (prompted). Username must be "admin"
 - the ssh key for controller-agent communication matching pubkey given to agent (global credentials)
 - the github personal access token as username/password (global credentials)

If a jenkins volume already exists, the configuration script will reset configuration values like credentials.

USAGE:
docker build -t jenkins:jcasc .
docker run -d --name jenkins --rm --network jenkins --volume jenkins-data:/var/jenkins_home --publish 8080:8080 --publish 5000:5000 jenkins:jcasc