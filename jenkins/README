In the run command, you must fill in:
 - the jenkins admin id
 - the jenkins admin password
 - the ssh private key location
 - the ssh public key location
 - the ssh key password

USAGE:
update server ip address in casc.yaml (      url: http://128.32.95.154/:8080/)
docker build -t jenkins:jcasc .
docker run --name jenkins --rm --network jenkins --publish 8080:8080 --publish 5000:5000 --env JENKINS_ADMIN_ID=admin --env JENKINS_ADMIN_PASSWORD=password --env SSH_PRIVATE_KEY="`cat /home/srichers/.ssh/jenkins_agent_key`" --env SSH_KEY_PASSWORD="" --env SSH_PUBLIC_KEY="`cat /home/srichers/.ssh/jenkins_agent_key.pub`" jenkins:jcasc